# Core ML / DL
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
scipy>=1.11.0

# Clinical risk models (Module 2)
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.0
openpyxl>=3.1.0

# Image processing
opencv-python-headless>=4.9.0
Pillow>=10.2.0
albumentations>=1.4.0

# Report generation (Module 5) — uses Ollama (local, free) via HTTP;
# Ollama itself is installed separately (https://ollama.com), not via pip.
requests>=2.31.0

# Explainability (Module 3)
grad-cam>=1.5.0
# Note: shap is intentionally NOT listed here — it depends on numba, whose
# compiled DLL gets blocked by Windows Application Control on this dev
# machine. src/explainability/shap_risk.py implements the same idea
# (per-prediction tree contribution explanations) with plain NumPy instead.

# Metrics / plotting
matplotlib>=3.8.0
seaborn>=0.13.0

# Experiment tracking
tensorboard>=2.16.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1

# API (Module 7, wired in later)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9